using static Sorting;
using static Infrastructure;

// int[] array = CreatArray(10);
// Show(array);
// SortSelection(array);
// Show(array);

10.CreatArray(min: 10, max: 30)
  .Show()
  .SortSelection()
  .Show();